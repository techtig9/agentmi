import { createPlatformTool } from "@/lib/actions/platform-tools";
export function CreateToolForm({secrets}:{secrets:{id:string;name:string;provider:string}[]}){return <form action={createPlatformTool} className="neon-card p-5 space-y-3"><div><p className="font-bold">Create custom tool</p><p className="text-xs text-ink-600 mt-1">Start with a safe HTTP tool. Attach a secret below to authenticate its requests.</p></div><input name="name" required placeholder="Tool name" className="w-full bg-base-800 border border-base-700 rounded-lg px-3 py-2 text-sm"/><textarea name="description" placeholder="What does it do?" className="w-full bg-base-800 border border-base-700 rounded-lg px-3 py-2 text-sm"/><div className="grid grid-cols-2 gap-2"><select name="kind" className="bg-base-800 border border-base-700 rounded-lg px-3 py-2 text-sm"><option value="http">HTTP API</option><option value="web_search">Web Search</option><option value="calculator">Calculator</option><option value="custom">Custom</option></select><select name="method" className="bg-base-800 border border-base-700 rounded-lg px-3 py-2 text-sm"><option>GET</option><option>POST</option><option>PUT</option><option>PATCH</option><option>DELETE</option></select></div><input name="endpoint" type="url" placeholder="https://api.example.com/action (optional)" className="w-full bg-base-800 border border-base-700 rounded-lg px-3 py-2 text-sm"/><label className="text-xs text-ink-500 block">Authenticate with<select name="secret_id" className="mt-1 w-full bg-base-800 border border-base-700 rounded-lg px-3 py-2 text-sm"><option value="">No secret (unauthenticated)</option>{secrets.map(s=><option key={s.id} value={s.id}>{s.name} ({s.provider})</option>)}</select></label><button className="btn-primary">Create Tool</button></form>}
